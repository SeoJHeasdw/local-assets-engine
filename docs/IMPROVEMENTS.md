# 다음 개선 후보

2026-09-17 오전 작업 종료 시점의 메모다. 다음 작업의 범위를 고를 때 참고하며, 아래 "남은 후보"가
이미 구현됐다는 뜻은 아니다. 구조와 계약은 [ARCHITECTURE](ARCHITECTURE.md), 화면 규칙은
[DESIGN_SYSTEM](DESIGN_SYSTEM.md), 채택 근거는 [DECISIONS](DECISIONS.md)를 따른다.

## 유지할 방향

- 1차 목표는 고품질 2D·3D 원본을 안정적으로 만드는 제작 도구다.
- 게임용 파일은 기본 생성에 포함하지 않는다. 실제 게임의 요구가 정해진 뒤 별도로 최적화한다.
- 원본과 이전 버전을 보존하고, 편집 결과는 새 버전으로 저장한다.
- 주 화면은 결과와 제작 동작에 집중한다. 상세 시간·메모리·로그는 기록으로 유지한다.
- 모델·해상도·면 수 등 품질 기본값 변경은 같은 입력·시드의 비교와 사용자 승인 후 진행한다.

## 이번에 끝낸 것 (2026-09-17)

| 요청·후보 | 결과 |
| --- | --- |
| 창을 머리글로 끌어 옮기기 | 맨 위 띠와 모든 화면 머리글이 창 끌기 영역. 단추·입력은 그대로 눌림 |
| 이미지·3D의 승인·거절 | 편집 화면·보관함에서 없애고 즐겨찾기·컬렉션·태그·메모로 교체. 프리비즈 컷 승인은 유지 |
| 색·로고가 부위를 고르지 못함 | 편집을 레이어로 바꿈. 색 바꾸기에 브러시 영역, 로고·문구 여러 개, 3D 로고는 이어진 표면에만 |
| 생성 요청 초안 저장 | 설명·스타일·설정·고른 이미지를 앱 재시작 후 복원 |
| 후보 나란히 비교·선택 | `나란히 비교` 팝업: 크게 보기·←→·숫자키, 고른 이유를 메모로 저장, 바로 3D 변환 |
| 버전 이력 화면 | 원본 → 재구성 → 편집 갈래를 나무로 표시, 어느 버전에서든 새로 편집 |
| 즐겨찾기·태그·컬렉션 | 보관함 필터·태그 칩·검색, 편집본이 컬렉션·태그를 이어받음 |
| 작업 완료 알림·대기 상태 | 창이 가려져 있으면 macOS 알림, 앞에 남은 작업 수, 대기 시간과 처리 시간 분리 |
| 보관 용량 안내 | 환경 화면에 분류별 크기와 큰 작업 목록, 중간 파일·작업을 휴지통으로(되살림 가능) |
| 로고 직접 조작 손잡이 | 2D 상자 끌기·모서리 크기·회전 손잡이·가운데 스냅·화살표 이동, 3D 표면 끌기·Alt/Shift+휠 |
| 여러 로고·문구 레이어 | 보이기·잠금·순서·삭제·이름, 문구 색·글꼴을 저장 후에도 다시 고침 |
| 영역을 지정하는 색상 교체 | 2D·3D 브러시와 지우개, 칠한 곳 표시, "비슷한 색만"/"영역 전체 칠하기" |
| 곡면과 경계의 로고 처리(일부) | 이어진 표면 제한, 투영 깊이 조절, UV 섬 경계 여백까지 번지게 해 이음매 선 제거 |
| 자유 자르기·캔버스 구성 | 비율 칩과 자르기 틀, 여백·배경색, 출력 가로 크기 |
| 실행 취소·다시 실행 | ⌘Z/⇧⌘Z, 슬라이더 조작 묶기, 단계를 골라 돌아가는 작업 내역 |

검증 근거: 앱 39개·Python 102개 테스트와 구조 검사 통과. 앱 창(오프스크린)에서 1440×900과 최소 1040×700
화면을 찍어 확인했다. 100만 면 상자 사본으로 브러시 영역 + 자물쇠 문구를 저장해 Blender 렌더가 미리보기와 같음,
예전 편집본이 레이어로 같은 모습으로 열림, 앱을 완전히 끄고 다시 열어 미저장 편집이 복원됨, 다른 렌더가 도는
중 저장하면 대기 상태가 보이고 취소하면 편집 화면이 다시 풀림을 확인했다.

## 먼저 확인할 항목

| 항목 | 확인할 내용 |
| --- | --- |
| 실제 앱의 창 끌기와 알림 | 오프스크린 창으로는 끌기와 macOS 알림 표시를 확인할 수 없다. 앱을 다시 열어 머리글 끌기, 알림 권한·클릭 동작 확인 |
| 휴지통 보내기 | 앱에서 중간 파일 정리·작업 휴지통 보내기를 한 번 실행하고 Finder 휴지통에서 되살려 목록에 돌아오는지 |
| 실제 마우스 조작 | 합성 포인터 이벤트로만 끌기·브러시를 확인했다. 트랙패드 드래그·Alt+휠·[ ] 브러시 크기 체감 확인 |
| 여러 형태의 3D | 상자 외에 곡면·얇은 물체·깊은 홈·구멍이 있는 물체에서 브러시 영역과 이어진 표면 로고가 안정적인지 |
| 긴 브러시 편집 | 획이 수천 개일 때 미리보기 속도(1024px 기준 200획 약 0.1초)와 4K 저장 시간 |

## 남은 후보

| 우선순위 | 후보 | 범위와 주의점 |
| --- | --- | --- |
| 중간 | 부위 자동 선택 | 브러시 대신 클릭 한 번으로 뚜껑·자물쇠를 고름. 분할 모델(SAM 계열 등)의 상업·한국 이용 조건부터 확인. 결과는 `region` 마스크 자리에 넣는다 |
| 중간 | 곡면을 따라 감싸는 로고 | 원기둥·구에서 평면 투영이 늘어나는 문제. 표면 거리 기반 펼침과 왜곡 표시가 필요 |
| 중간 | 3D 로고 화면 손잡이 | 지금은 표면 끌기와 휠. 화면에 투영한 크기·회전 손잡이는 카메라 투영 계산이 필요 |
| 중간 | 보관함 여러 개 선택 | 여러 에셋에 한 번에 컬렉션·태그 붙이기, 작업 여러 개 휴지통으로 |
| 낮음 | 예상 남은 시간 | 측정 기록의 중앙값으로 대기·처리 남은 시간 추정 |
| 이후 | 실제 음각·양각·형상 편집 | 현재 로고는 텍스처에 표현하는 표면 편집이다. 형상을 깎는 기능은 별도 설계·검증 필요 |
| 이후 | 자연어 부분 수정 | 색·표면 편집을 넘어 형태를 바꾸는 생성 편집. 원본 유지, 로컬 자원, 라이선스부터 검토 |

## 게임 제작이 시작될 때

게임용 최적화는 현재 원본 생성 품질과 별도의 작업으로 둔다.

- 목표 엔진·플랫폼·카메라 거리·동시 표시 개수를 먼저 정한다.
- 원본의 윤곽을 유지한 경량 메시와 노멀맵 베이킹을 비교한다.
- 거리별 모델(LOD), 텍스처·메시 압축, 반복 배치 시 데이터 공유를 검토한다.
- 파일 용량만 보지 않고 실제 화면 차이·메모리·렌더링 시간을 함께 측정한다.
- 현재 과거 작업의 ‘게임용’ 파일을 최종 배포 최적화 완료본으로 간주하지 않는다.
